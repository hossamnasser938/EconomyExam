import { StyleSheet } from 'react-native';

const styles = StyleSheet.create( {
    input: {
        width: "100%",
        height: 40,
        borderColor: "#123",
        borderWidth: 2
    }
} );

export default styles;