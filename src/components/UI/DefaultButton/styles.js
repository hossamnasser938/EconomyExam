import { StyleSheet } from 'react-native';

const styles = StyleSheet.create( {
    container: {
        padding: 5,
        backgroundColor: "#123",
        borderColor: "black",
        borderWidth: 3,
        borderRadius: 5
    },
    buttonTitle: {
        fontSize: 18,
        color: "white"
    }
} );

export default styles;